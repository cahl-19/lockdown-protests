<div class="modal" tabindex="-1" role="dialog" id="{{id_prefix}}-form-modal">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h3 class="modal-title text-center">{{form_title}}</h3>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <div class="protest-location">
            <small>Protest Located At: <span id='{{id_prefix}}-display-location'></span></small>
        </div>
        <form class="needs-validation" id='{{id_prefix}}-form' autocomplete="off" novalidate>

            <input type="hidden" id="{{id_prefix}}-input-latitude">
            <input type="hidden" id="{{id_prefix}}-input-longitude">
            <input type="hidden" id="{{id_prefix}}-input-protest-id">

            <div class="form-group">
              <label for="{{id_prefix}}-input-title">Title</label>
              <input
                  type="text" class="form-control" id="{{id_prefix}}-input-title"
                  aria-describedby="{{id_prefix}}-title-help" placeholder="Enter Protest Title" required
              >
              <small id='{{id_prefix}}-title-help' class="form-text text-muted">
                  Max 256 characters.
              </small>
            </div>
            <div class="form-group has-validation">
                <label for="{{id_prefix}}-input-date">Date:</label>
                <input type="date" id="{{id_prefix}}-input-date" required>
                <input type="time" id="{{id_prefix}}-input-time" value="15:00" required>
                <div><small class="form-text text-muted">
                    It is currently <span id="{{id_prefix}}-user-current-time"></span>
                </small></div>
                <div class="invalid-feedback" id="{{id_prefix}}-date-time-validity-feedback">
                    Date & Time are Required.
                </div>
            </div>
            <div class="form-group">
              <label for="{{id_prefix}}-input-dress-code">Dress Code (Optional but Recommended )</label>
              <input
                  type="text" class="form-control" id="{{id_prefix}}-input-dress-code"
                  aria-describedby="{{id_prefix}}-dress-code-help" placeholder=""
              >
              <small id='{{id_prefix}}-dress-code-help' class="form-text text-muted">
                  Max 128 characters.
              </small>
            </div>
            <div class="form-group">
              <label for="{{id_prefix}}-input-description">Description</label>
              <textarea
                  class="form-control" id="{{id_prefix}}-input-description"
                  rows="4" aria-describedby="{{id_prefix}}-protest-description-help" required
              ></textarea>
              <small id='{{id_prefix}}-protest-description-help' class="form-text text-muted">
                  Keep it legal, linking  to external sites is allowed and <em>encouraged</em>. Less than 768
                  characters.
                  <br/><br/>
                  Required: describe the meeting spot (or address), type of protest (rally, march, sit-in etc.) and
                  contact info (but don't dox yourself unless you understand the risks).
              </small>
            </div>
        </form>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" id="{{id_prefix}}-form-submit">Submit</button>
        {{#if include_delete}}
            <button type="button" class="btn btn-danger" id="{{id_prefix}}-protest-delete">Delete</button>
        {{/if}}
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
      </div>
    </div>
  </div>
</div>